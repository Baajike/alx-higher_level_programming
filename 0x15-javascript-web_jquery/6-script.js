// Use the JQuery API to add a click event listener to the div with id 'update_header'
$('#update_header').click(function() {
    // Use the JQuery API to select the <header> element and update its text
    $('header').text('New Header!!!');
});

